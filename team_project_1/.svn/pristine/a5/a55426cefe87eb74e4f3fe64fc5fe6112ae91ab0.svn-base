package xyz.for01.reservation;

public class RequestResVo {
	SELECT RES_IDX, NAME, PHONE, RES_START, RES_END, GET_HOTELNAME(HOTELNO), GET_ROOMNAME(ROOMNO), RESDATE
	FROM RESERVATION 
	WHERE HOTELNO = ANY(SELECT HOTELNO FROM HOTEL WHERE ADMIN = 161) 
	AND RESSTAT = 0
	ORDER BY RESDATE;
}
